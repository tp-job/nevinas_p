# 🖼️ Gallery Application

แอปพลิเคชันสำหรับจัดการและแสดงรูปภาพ พร้อม MongoDB backend และ React frontend

## 🎯 Features

- ✅ แสดงรูปภาพทั้งหมดจาก MongoDB
- ✅ Responsive Grid Layout
- ✅ Lightbox สำหรับดูรูปขนาดใหญ่
- ✅ Navigation ด้วยปุ่มและคีย์บอร์ด
- ✅ อัพโหลดรูปใหม่ได้
- ✅ Sync รูปจาก folder เข้า database อัตโนมัติ

## 📦 สถานะปัจจุบัน

✅ **รูปภาพใน Database**: 30 รูป
- 29 รูปจาก folder `server/uploads/images/`
- 1 รูปที่อัพโหลดทดสอบ

## 🚀 Quick Start

### วิธีที่ 1: ใช้ Batch Script (Windows)

เพียงแค่ดับเบิลคลิก:
```
start-gallery.bat
```

### วิธีที่ 2: รันด้วยตนเอง

#### 1. รัน Backend
```bash
cd server
npm start
```

#### 2. รัน Frontend (terminal ใหม่)
```bash
cd client
npm run dev
```

#### 3. เปิดเบราว์เซอร์
```
http://localhost:5173/gallery
```

## 🔄 Sync รูปใหม่

หากเพิ่มรูปใหม่ใน `server/uploads/images/` และต้องการเพิ่มเข้า database:

### วิธีที่ 1: ใช้ Batch Script
```
sync-images.bat
```

### วิธีที่ 2: รันด้วย Command
```bash
cd server
node src/syncGallery.js
```

## 📂 โครงสร้างโปรเจค

```
nevinas_ka_i/
├── server/
│   ├── uploads/
│   │   └── images/              # รูปภาพทั้งหมด (29 รูป)
│   ├── src/
│   │   ├── models/
│   │   │   └── Gallery.js       # MongoDB Schema
│   │   ├── syncGallery.js       # Script สำหรับ sync รูป
│   │   └── server.js            # Express API Server
│   └── .env                     # MongoDB connection string
│
├── client/
│   └── src/
│       └── pages/
│           └── Gallery.tsx      # Gallery Component
│
├── docs/
│   └── gallery-setup.md         # เอกสารรายละเอียด
│
├── start-gallery.bat            # เริ่มต้นแอพ (Windows)
├── sync-images.bat              # Sync รูปเข้า DB (Windows)
└── GALLERY-README.md            # ไฟล์นี้
```

## 🛠️ API Endpoints

### GET `/api/gallery`
ดึงรูปภาพทั้งหมด

**Response:**
```json
[
  {
    "_id": "...",
    "name": "000002.JPG",
    "img": "/uploads/images/000002.JPG",
    "created_at": "2025-12-07T..."
  }
]
```

### POST `/api/gallery/upload`
อัพโหลดรูปใหม่

**Request:**
- Content-Type: `multipart/form-data`
- Field: `image` (file)

**Response:**
```json
{
  "success": true,
  "message": "Image uploaded successfully",
  "data": {
    "_id": "...",
    "name": "...",
    "img": "/uploads/images/..."
  }
}
```

## 🎨 Gallery Features

### Grid View
- Responsive grid layout (1-3 columns ตามขนาดหน้าจอ)
- Hover effect (ซูมเล็กน้อย)
- Lazy loading สำหรับประสิทธิภาพ

### Lightbox
- คลิกรูปเพื่อดูขนาดใหญ่
- ปุ่ม Previous/Next
- แสดงตำแหน่งรูป (เช่น 1/30)
- ปิดด้วยปุ่ม X หรือคลิกพื้นหลัง

### Keyboard Shortcuts
- `Escape` - ปิด lightbox
- `Arrow Left` - รูปก่อนหน้า
- `Arrow Right` - รูปถัดไป

### Upload
- เลือกไฟล์จากเครื่อง
- แสดงสถานะการอัพโหลด
- Refresh gallery อัตโนมัติหลังอัพโหลดสำเร็จ

## 💾 Database

**Database Name:** `nevinas_db`  
**Collection:** `galleries`  
**Connection:** `mongodb://localhost:27017`

### Schema
```javascript
{
  name: String,        // ชื่อไฟล์ (เช่น 000002.JPG)
  img: String,         // path ของรูป (เช่น /uploads/images/000002.JPG)
  created_at: Date     // วันที่สร้าง (default: Date.now)
}
```

## 📝 รายการรูปภาพ

รูปทั้งหมด 29 รูปที่ sync เข้า database:
- 000002.JPG
- 000003.JPG
- 000005.JPG
- 000006.JPG
- 000016.JPG - 000021.JPG
- 000024.JPG - 000027.JPG
- 000029.JPG - 000043.JPG

## 🔧 Troubleshooting

### รูปไม่แสดง
1. ตรวจสอบว่า backend server รันอยู่ที่ `http://localhost:3000`
2. เช็ค MongoDB ว่าเชื่อมต่อสำเร็จ
3. ตรวจสอบว่ารูปอยู่ใน `server/uploads/images/`

### Sync ไม่สำเร็จ
1. ตรวจสอบ MongoDB connection string ใน `server/.env`
2. ตรวจสอบว่า folder `server/uploads/images/` มีอยู่
3. ตรวจสอบสิทธิ์การอ่านไฟล์

### Upload ไม่สำเร็จ
1. ตรวจสอบว่าไฟล์เป็นรูปภาพ (jpg, jpeg, png, gif, webp)
2. ตรวจสอบ backend server ว่ารันอยู่
3. ดู console ใน browser สำหรับ error message

## 📚 เอกสารเพิ่มเติม

- [Gallery Setup Guide](docs/gallery-setup.md) - รายละเอียดการตั้งค่า
- [Database Documentation](docs/database.md) - โครงสร้าง database

## ✨ สรุป

การตั้งค่า Gallery เสร็จสมบูรณ์แล้ว! รูปภาพทั้งหมด 29 รูปจาก folder `server/uploads/images/` ถูกเพิ่มเข้า MongoDB แล้ว และพร้อมแสดงใน Gallery.tsx

เพียงแค่รัน `start-gallery.bat` และเปิดเบราว์เซอร์ไปที่ `http://localhost:5173/gallery` ก็จะเห็นรูปภาพทั้งหมดแล้วครับ! 🎉

